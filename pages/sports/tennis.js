import ScoreBoard from "../../components/layout/ScoreBoard";

function Tennis() {
  return (
    <div>
      <ScoreBoard pageInfo={'tennis'} title={'Tennis News'} description={'Catch up on the latest on the baseline...'} />
    </div>
  );
}

export default Tennis;